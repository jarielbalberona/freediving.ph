# Spec Coverage Matrix (FPH v1.1)

Legend: `Implemented` = code exists and enforcement observed. `Partial` = core paths exist but boundaries missing/incomplete. `Missing` = no reliable implementation found.

| Spec Item | Status | Evidence (file/route/symbol) | Risks | Fix Summary |
|---|---|---|---|---|
| Profiles | Partial | Read/update profile: `apps/api/src/app/profiles/profiles.routes.ts`, `ProfilesService.getByUsername`, `updateOwnProfile` in `apps/api/src/app/profiles/profiles.service.ts` | Blocking not enforced across all visibility surfaces; coarse-location policy not enforced by validation | Enforce location granularity in profile update validator/service; centralize visibility+block policy helper and apply to all profile-adjacent reads |
| PBs (Personal Bests) | Implemented | Create/update/delete own PBs: `POST/PUT/DELETE /profiles/me/personal-bests*` in `apps/api/src/app/profiles/profiles.routes.ts`; service methods in `apps/api/src/app/profiles/profiles.service.ts` | No audit logging for PB moderation/deletes; no anti-spam limits for PB writes | Add PB write rate limits, moderation hooks, and audit logs for sensitive actions |
| Activity Feed | Missing | `profile_activity_items` table exists in `apps/api/src/models/drizzle/profiles.model.ts`; no route/service using it found | Spec event feed missing; visibility rules for feed cannot be enforced | Implement feed write/read service with visibility filtering and block-aware feed query |
| Messaging | Partial | Routes and service: `apps/api/src/app/messages/messages.routes.ts`, `apps/api/src/app/messages/messages.service.ts` (`createOrGetDirectConversation`, `sendMessage`, membership checks) | No explicit content sanitization; moderation limited to one endpoint; no pagination metadata in list endpoints | Add output-safe rendering contract, consistent pagination envelope, moderation reason capture for all remove paths |
| Buddy System | Partial | Request lifecycle and finder: `apps/api/src/app/buddies/buddies.service.ts`; routes in `apps/api/src/app/buddies/buddies.routes.ts` | No explicit audit logs for sensitive buddy actions; no user-facing block CRUD API found | Add `/blocks` API with scope support, audit logs for request/reject/remove, and stricter conflict handling |
| Groups | Partial | CRUD/membership/posts in `apps/api/src/app/groups/*` | Critical auth gaps: add/remove member/post creation can be abused; actor role checks inconsistent | Enforce server-side ownership/group-role checks; remove user-supplied `authorId`; log moderation/administrative actions |
| Chika (normal) | Partial | Thread/comments/reactions: `apps/api/src/app/threads/*` | Thread update/delete missing owner/moderator check; block visibility not consistently applied across all related resources | Add ownership checks for update/delete and centralize block/visibility guard for every read/write path |
| Chika (pseudonymous) | Partial | Pseudonym schema: `apps/api/src/models/drizzle/chika.model.ts`; mode update + pseudonym creation in `ThreadsService.setThreadMode` and `getOrCreatePseudonym` | Identity leak in thread/comment responses; pseudonymous rate counter counts all comments, not pseudo-only; no category-level mode model as spec describes | In read responses, replace author identity with thread pseudonym when mode is pseudonymous; count pseudo replies by joining mode table; keep reveal restricted to mod/admin endpoint |
| Explore + Dive Sites + community submission moderation | Partial | Dive spot submit/review: `apps/api/src/app/diveSpot/diveSpot.routes.ts`, `DiveSpotService.createDiveSpot/reviewDiveSpot` | Coarse-location non-negotiable not enforced (`lat/lng` accepted and returned); no report linkage to moderation queue | Store coarse region/public location separately from precise coords; hide precise coords by default; integrate report-to-review workflow |
| Buddy Finder | Partial | Finder query + block filtering: `BuddiesService.finder` in `apps/api/src/app/buddies/buddies.service.ts` | Returns `location` and `homeDiveArea` directly (possibly too precise); no coarse normalization policy | Add coarse-location transform at write-time and finder response-time |
| Events | Partial | Event CRUD/attendees: `apps/api/src/app/events/*` | Attendee operations allow acting on other users (`userId` trusted from request body/params); enum mismatch between validators/model | Force `userId` from auth context for self-join/leave; add organizer/mod checks for managing others; align enum contracts |
| Competitive Records | Partial | Submit/list/moderate: `apps/api/src/app/competitiveRecords/*` | Moderate path lacks reason-code taxonomy; no report linkage; no explicit state tombstone placeholder on remove | Extend moderation payload to include reasonCode + note, add visibility state fields/placeholders, add report queue integration |
| Reporting / Moderation | Partial | Reports: `apps/api/src/app/reports/*`; moderation actions: `apps/api/src/app/moderation/*`; reason enums: `apps/api/src/models/drizzle/moderation.model.ts` | Reporting not visibly enforced across all modules; placeholders for removed content inconsistent by module | Add moderation adapters per content type and enforce standardized removal placeholder conventions |
| RBAC | Partial | `requireRole`/`requirePolicy`: `apps/api/src/middlewares/clerk.middleware.ts`; policy map `apps/api/src/core/policies.ts` | Role mapping is inconsistent with spec (`USER/EDITOR/ADMINISTRATOR` vs spec guest/member/moderator/admin); authorization drift across modules | Introduce canonical permission matrix and shared authorizer utility; replace ad-hoc route checks |
| Rate Limits | Partial | Global limiter: `apps/api/src/rateLimiter.ts`; feature limits in routes + `ABUSE_LIMITS` (`apps/api/src/core/abuseControls.ts`) | Not all write surfaces rate-limited; pseudonymous limits are not scoped correctly; no central metric/alert on throttling | Apply per-feature limiters to all mutation routes; fix pseudo counters; emit limiter metrics |
| Privacy defaults (coarse location, visibility defaults, block visibility) | Partial | Profile visibility fields in `users` table (`apps/api/src/models/drizzle/authentication.model.ts`); block helpers (`apps/api/src/core/blocking.ts`) | Coarse location default not enforced; block policy not global; pseudonym identity leak | Add privacy policy layer for serialization + query filtering across modules; enforce coarse location schema |
| Soft delete + anonymization on account deletion | Partial | Anonymization implemented: `UserService.anonymizeUserAccount` in `apps/api/src/app/user/user.service.ts` | Clerk webhook hard-deletes users (`handleUserDeleted` in `apps/api/src/routes/clerk-webhook.ts`) conflicts with spec preference | Replace hard delete webhook behavior with anonymization/suspension flow; preserve referential integrity and community placeholders |
