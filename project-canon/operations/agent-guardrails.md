# Agent Guardrails

No random markdown.

Durable project truth belongs in `project-canon/`.

Root `docs/` is legacy by adoption policy and is not a normal final state.

App/package-local docs are allowed only when ownership is clearly local to that app/package.

Do not create project-local `.ai/`, `.agent/`, or `.codex/`.

Do not recreate `docs/` or `docs.legacy-review/` as project source-of-truth folders.

Appropriate verification is default. E2E is escalation, not default.

Do not claim PASS when the proof boundary was not actually exercised.
