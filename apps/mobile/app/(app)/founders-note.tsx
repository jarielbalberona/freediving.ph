import { NavPlaceholderScreen } from "@/features/readiness/nav-placeholder-screen";

export default function FoundersNoteRoute() {
  return (
    <NavPlaceholderScreen
      description="The Founder’s Note is coming to mobile. Use the web app to read the current note."
      title="Founder’s Note"
    />
  );
}
