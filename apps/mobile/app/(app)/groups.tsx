import { NavPlaceholderScreen } from "@/features/readiness/nav-placeholder-screen";

export default function GroupsRoute() {
  return (
    <NavPlaceholderScreen
      description="Mobile group browsing is not ready yet. Use the web app for group pages while the mobile experience is being wired."
      title="Groups"
    />
  );
}
